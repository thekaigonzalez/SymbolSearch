# Check Symbols with SymbolSearch.
Run bin/SymbolSearch

Output with commands should be:

```

USER@USER-NAME:~/DIR/bin$ ./SymbolSearch ../obj/testobj.o main # After compiling the testobj.o
Yes
USER@USER-NAME:~/DIR/bin$ 

```